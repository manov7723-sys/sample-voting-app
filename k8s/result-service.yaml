apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: result
  namespace: dev
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
  type: ClusterIP
  selector:
    app: result
  ports:
    - name: result-service
      port: 80
      targetPort: 80
