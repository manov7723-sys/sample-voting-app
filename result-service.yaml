apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: result
spec:
  type: LoadBalancer
  selector:
    app: result
  ports:
    - name: result-service
      port: 80
      targetPort: 8081
