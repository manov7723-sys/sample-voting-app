apiVersion: v1
kind: Service
metadata:
  name: vote
  labels:
    app: vote
  namespace: dev
spec:
  type: LoadBalancer
  selector:
    app: vote
  ports:
    - name: vote-service
      port: 80
      targetPort: 80